{
  "name": "@matthewadams/js-standard-test",
  "version": "0.1.0-pre.0",
  "scripts": {
    "build": "npm install",
    "test": "run-s lint",
    "lint": "standard --verbose 'src/**/*.js'",
    "format": "standard --fix 'src/**/*.js'"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "standard": "13.1.0"
  }
}
